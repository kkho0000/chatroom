spring:
  application:
    name: chatroom
  datasource:
    url: jdbc:mysql://localhost:3306/chat_room_db
    username: root
    password: kkho0000
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect